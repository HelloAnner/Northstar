# NorthStar 项目指南

## 文档体系

### 全局设计文档 `docs/design/`

按阶段编号（`001`、`002`…），**优先阅读最新阶段**（当前为 `002`）：

| 文件            | 作用                                   | 参考权重         |
| --------------- | -------------------------------------- | ---------------- |
| `prd-00x.md`    | 功能设计，需求的来源                   | 高               |
| `tech-00x.md`   | 技术架构设计，由 prd 生成              | **最高**         |
| `plan-00x.md`   | 分阶段推进计划，已完成的阶段会在此标注 | 高               |
| `market-00x.md` | 市场分析，作者思路来源                 | 低（不影响实现） |

> 历史阶段（如 `001`）可作参考，但以最新阶段为准，无需深读。

---

### 业务模块文档 `docs/<模块名>/`

每个模块对应 `tech-00x.md` 中描述的业务模块，目录结构如下：

```
docs/<模块名>/
  <版本号>/
    prd.md        # 该模块的详细功能设计
    e2e.md        # E2E 验收标准（通过即视为完成）
    plans/        # 分阶段详细拆解计划
      <日期>-<描述>.md
```

---

## 开发流程

实现一个业务模块功能前，**必须先完成文档**，再写代码：

```
1. 阅读 docs/design/00x/tech-00x.md 了解全局架构
2. 补全目标模块的文档 ， 这是必须要做的事情， 判断是不是自动增加 00x：
   - docs/<模块>/00x/prd.md    — 详细功能设计
   - docs/<模块>/00x/e2e.md    — 验收标准
   - docs/<模块>/00x/plans/    — 分阶段计划拆解

注意一个 phase 可能对应多个模块， 需要做好实现顺序， 每一个模块都必须要先做好文档拆分，再实现功能。

3. 每一个模块都需要按 plans 分阶段实现前后端代码
4. 保证 e2e 全部通过
5. 在 docs/design/00x/plan-00x.md 中标注该阶段已完成
```

每完成一个阶段，**在 `plan-00x.md` 中更新完成状态**，逐步推进。

---
